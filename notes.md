- work on issues reported by fallow
- work on TODOs
- consider "WWUnit" naming scheme that avoids conflicts with other package imports
  - either adopt WW prefix for more types with conflicts or remove WW prefix
- read WW discord on what the issue was with either fog, sonja, or sasha regarding hidden units or something
- come up with rules (ESLint rules?) for `throw new Error` / TypeError / InvalidStateError / IllegalActionError / DispatchableError
  - enforce @throws JSdoc?

====

new schema mechanism things:

- properties get HP for capture progress, similar to pipe seams
- if a transport gets moved, it should update it's loaded units positions to stay in syncs
